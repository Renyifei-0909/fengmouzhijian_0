# 烽眸智鉴 · 产品功能文档

> 产品名称：多源感知赋能端边云协同的智能监管与可信交付系统  
> 文档用途：基于现有前端 Demo 页面，明确功能范围与前后端开发边界，作为后续联调与迭代依据  
> 文档版本：v1.0  
> 对应前端：烽眸智鉴 7.4（React + Vite + Tailwind）

---

## 1. 文档说明

### 1.1 背景

当前仓库为**高保真前端演示系统**，页面交互与数据展示已基本齐备，但数据来自本地 mock，关键操作多为前端模拟提示。本文件按现有页面拆解**业务功能、字段、交互、前后端职责与建议接口**，指导从 Demo 演进为可落地的前后端产品。

### 1.2 产品定位

面向通信基础设施建设场景，覆盖：

- **感知端**：摄像头、智能安全帽、传感器等数据采集
- **边缘侧**：违章识别、本地预警、弱网缓存
- **云端**：隐蔽工程 AI 验真、GIS 图物对齐、进度核算、哈希可信存证

### 1.3 页面与模块总览

| 模块 | 路由 | 前端页面 | 优先级 |
|------|------|----------|--------|
| 监管总览 | `/dashboard` | DashboardPage | P0 |
| 隐蔽验真 AI | `/ai-verification` | HiddenAIPage | P0 |
| 项目管理 | `/projects` | ProjectsPage | P0 |
| 项目详情 | `/projects/:id` | ProjectDetailPage | P0 |
| 告警中心 | `/alarms` | AlarmsPage | P0 |
| 设备监控 | `/devices` | DevicesPage | P0 |
| 数据分析 | `/analytics` | AnalyticsPage | P1 |
| 报表中心 | `/reports` | ReportsPage | P1 |
| GIS 地图 | `/gis-map` | GISMapPage | P1 |
| 数据看板 | `/data-cockpit` | DataCockpitPage | P1 |
| 溯源查询 | `/traceability` | TraceabilityPage | P0 |
| 模型服务 | `/model-service` | ModelServicePage | P1 |
| 账户设置 | `/settings/account` | AccountSettingsPage | P2 |
| 系统设置 | `/settings/system` | SystemSettingsPage | P2 |

### 1.4 业务主链路

```
设备采集 → 告警产生 → 项目监管
              ↓
       隐蔽验真 AI → 结构化结果 → 报表 / 数据看板
              ↓
       上链存证 / 数字档案 → 溯源查询
              ↕
         GIS 图物对齐 ←→ 项目详情
         模型服务为边缘/云端推理能力运营面
```

---

## 2. 全局能力

### 2.1 布局与导航

| 能力 | 说明 | 前端 | 后端 |
|------|------|------|------|
| 侧边栏主导航 | 监管总览、隐蔽验真、项目、告警、设备、分析、报表 | 路由与高亮态 | — |
| 快捷入口 | GIS、数据看板、溯源、模型服务 | 跳转 | — |
| 账户/系统设置入口 | 底部设置区 | 跳转 | — |
| 退出登录 | 清除会话并回登录页 | 调登出接口、清 Token | `POST /api/auth/logout` |
| 移动端快捷导航 | 小屏顶部横向导航 | 响应式 | — |
| 顶栏标题 | 按路由显示 title/subtitle | 路由 meta | — |
| 全局搜索 | 搜项目/设备/告警/档案 | 输入与结果页 | `GET /api/search?q=` |
| 通知中心 | 系统/告警通知列表 | 拉取与已读 | `GET/PATCH /api/notifications` |

### 2.2 登录与权限（Demo 缺失，需补齐）

| 能力 | 说明 | 前端 | 后端 |
|------|------|------|------|
| 登录 | 账号密码 / 短信（可选） | 登录页 | `POST /api/auth/login` |
| 会话 | Token / Cookie | 请求头携带、过期跳转 | JWT/Session 校验 |
| 角色权限 | 管理员 / 项目经理 / 现场人员等 | 菜单与按钮显隐 | RBAC 接口与中间件 |

---

## 3. 核心数据实体

### 3.1 Project（项目）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 主键 |
| name | string | 项目名称 |
| location | string | 施工地点 |
| manager | string | 负责人 |
| status | enum | `pending` / `active` / `paused` / `completed` |
| progress | number | 进度 0–100 |
| startDate | string | 开工日期 |
| endDate | string | 计划竣工日期 |
| participants | number | 参与人数 |
| cameras | number | 监控点位数 |
| alerts | number | 活跃告警数 |
| description | string | 项目说明 |

### 3.2 Device（设备）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 主键 |
| name | string | 设备名称 |
| type | enum | `camera` / `sensor` / `edge` / `helmet` |
| status | enum | `online` / `offline` / `warning` / `fault` |
| location | string | 部署位置 |
| lastHeartbeat | datetime | 最近心跳 |
| battery | number? | 电量（可选） |
| projectId | string | 所属项目 |

### 3.3 Alarm（告警）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 主键 |
| type | enum | `safety` / `quality` / `progress` / `device` |
| deviceId | string | 关联设备 |
| deviceName | string | 设备名称冗余 |
| projectId | string | 关联项目 |
| projectName | string | 项目名称冗余 |
| level | enum | `info` / `warn` / `critical` |
| message | string | 告警内容 |
| timestamp | datetime | 发生时间 |
| image | string | 抓拍图 URL |
| acknowledged | boolean | 是否已处理 |

### 3.4 Report（报表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 主键 |
| name | string | 报表名称 |
| type | enum | `weekly` / `monthly` / `quality` / `safety` / `progress` |
| projectId | string | 关联项目 |
| projectName | string | 项目名称 |
| period | string | 统计周期 |
| createdBy | string | 生成人 |
| createdAt | datetime | 生成时间 |
| size | string | 文件大小 |
| status | enum | `generated` / `pending` |
| fileUrl | string | 下载地址（后端补充） |

### 3.5 AIVerification（验真任务/结果，前端本地实体需落库）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 任务 ID |
| sceneType | enum | `duct` / `trench` / `cable` |
| sceneName | string | 场景名称 |
| location | string | 采集地点 |
| coverImage | string | 原图 |
| overlayImage | string? | AI 叠加图 |
| score | number | 综合得分 |
| status | enum | `pass` / `warn` / `fail` |
| summary | string | 结论摘要 |
| confidence | number | 置信度 |
| depth | string | 埋深 |
| spacing | string | 间距 |
| specification | string | 规格 |
| quantity | number | 数量 |
| riskPoints | string[] | 风险点 |
| suggestions | string[] | 整改建议 |
| hash | string | SHA-256 指纹 |
| projectId | string? | 关联项目 |
| createdAt | datetime | 创建时间 |

### 3.6 DigitalArchive（数字档案 / 溯源）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 档案编号，如 `ARC-2026-001` |
| project | string | 所属项目 |
| hash | string | SHA-256 |
| timestamp | datetime | 归档时间 |
| status | string | 如「可信有效」 |
| location | string | 归属位置 |
| summary | string | 摘要 |
| timeline | object[] | 全流程节点（时间、事件、操作者） |

### 3.7 ModelService（模型服务）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 模型 ID |
| name | string | 模型名称 |
| version | string | 版本号 |
| accuracy | number | 准确率 |
| latency | number | 推理时延 ms |
| status | enum | `running` / `idle` / `updating` |
| scene | string | 适用场景 |

### 3.8 User / SystemConfig

**用户资料**：name、phone、email、department、role、avatar、2FA 状态、最近登录  

**通知偏好**：alarm、weekly、login、sms（布尔）  

**系统配置**：networkMode（端边云协同 / 边缘优先 / 云端集中）、retention（数据保留天数）、logLevel、weakNet、audit、cache、autoArchive

---

## 4. 分模块功能说明（含前后端）

说明约定：

- **前端**：页面展示、交互、校验、调用接口、状态反馈  
- **后端**：数据持久化、业务规则、文件/任务、权限、推送  
- **接口建议**为 REST 风格示例，可按团队规范调整

---

### 4.1 监管总览 `/dashboard`（P0）

#### 功能描述

监管驾驶舱首页：展示平台 KPI、能力介绍、快捷入口、项目进度排名与最新告警。

#### 功能清单

| 编号 | 功能点 | 前端 | 后端 |
|------|--------|------|------|
| D-01 | KPI 卡片：在建项目、在线设备、今日告警、平均进度 | 渲染卡片与涨跌文案 | `GET /api/dashboard/stats` |
| D-02 | Banner 摘要：设备在线比、活跃项目、告警、进度 | 绑定 stats | 同上聚合接口 |
| D-03 | 隐蔽验真能力介绍区 | 静态文案 + 跳转验真/溯源 | — |
| D-04 | 快捷入口：GIS / 看板 / 溯源 / 模型 | 路由跳转 | — |
| D-05 | 项目进度排名图 | 条形图 | `GET /api/analytics/project-progress` |
| D-06 | 最新告警列表（Top N） | 列表 + 跳转告警中心 | `GET /api/alarms?limit=5&sort=time_desc` |

#### 验收标准

- 进入首页可看到真实或可配置的 KPI，刷新后数据一致  
- 告警点击可进入告警中心；快捷入口可正确跳转

---

### 4.2 隐蔽验真 AI `/ai-verification`（P0）

#### 功能描述

对隐蔽工程影像做结构化 AI 验真：场景选择 → 五步分析 → 参数/结论输出 → 对比预览 → 上链与整改。

#### 功能清单

| 编号 | 功能点 | 前端 | 后端 |
|------|--------|------|------|
| A-01 | 三场景切换：管道排布 / 沟槽开挖 / 线缆预埋 | 场景卡片 UI | 场景模板配置接口（可选） |
| A-02 | 上传/选用样例影像 | 上传组件、预览 | `POST /api/ai/tasks` 上传影像 |
| A-03 | 五步分析流程动画 | 步骤态展示 | 异步任务状态轮询 `GET /api/ai/tasks/:id` |
| A-04 | 输出得分、结论、置信度、埋深/间距/规格/数量 | 结果面板 | 推理服务写入结果字段 |
| A-05 | 风险点与整改建议 | 列表展示 | 规则引擎/模型输出 |
| A-06 | 原图与 AI 叠加对比 Modal | Modal 对比 | 返回原图 URL + overlay URL |
| A-07 | 结构化数据预览表 | 表格 Modal | 返回标准 JSON 字段 |
| A-08 | 推送到业务系统 | 按钮确认 | `POST /api/ai/tasks/:id/push` |
| A-09 | 导出 AI 报告 | 触发下载 | `GET /api/ai/tasks/:id/report` |
| A-10 | 打印验真单 | `window.print` 或 PDF | PDF 生成接口（可选） |
| A-11 | 哈希上链存证 | 展示 hash + 成功提示 | `POST /api/ai/tasks/:id/notarize` |
| A-12 | 发起整改复检 | 创建复检工单 | `POST /api/ai/tasks/:id/recheck` |

#### 关键字段（结果）

`score, status(pass/warn/fail), summary, confidence, depth, spacing, specification, quantity, riskPoints[], suggestions[], hash`

#### 验收标准

- 可选择场景并完成一次验真任务，结果可持久化查询  
- 上链后可在溯源模块按档案号/哈希检索到记录

---

### 4.3 项目管理 `/projects`（P0）

#### 功能描述

工程全景列表：状态筛选、卡片展示、新建项目、进入详情。

#### 功能清单

| 编号 | 功能点 | 前端 | 后端 |
|------|--------|------|------|
| P-01 | 项目卡片列表 | 网格卡片 | `GET /api/projects` |
| P-02 | 状态筛选：全部/进行中/待启动/已暂停/已完成 | Tab 过滤 | `?status=` |
| P-03 | 新建项目 Modal | 表单校验 | `POST /api/projects` |
| P-04 | 点击进入详情 | 路由 `/projects/:id` | — |

#### 新建表单字段

| 字段 | 必填 |
|------|------|
| name 项目名称 | 是 |
| location 施工地点 | 是 |
| manager 负责人 | 是 |
| description 项目说明 | 否 |

#### 列表展示字段

`name, location, status, description, progress, cameras, alerts, startDate, endDate`

#### 验收标准

- 新建后列表立即可见；非法空字段不可提交  
- 筛选与状态标签显示正确

---

### 4.4 项目详情 `/projects/:id`（P0）

#### 功能描述

单项目监管：总览阶段、关联设备、关联报表、可信档案，以及打印/导出/采集/跳转验真。

#### 功能清单

| 编号 | 功能点 | 前端 | 后端 |
|------|--------|------|------|
| PD-01 | 头信息与 KPI | 展示 progress、participants、cameras、设备数、告警数 | `GET /api/projects/:id` + 聚合 |
| PD-02 | Tab 总览：阶段进度条 | 设计/施工/隐蔽验真/验收/交付 | `GET /api/projects/:id/stages` |
| PD-03 | Tab 设备列表 + 详情 Modal | 表格与弹窗 | `GET /api/projects/:id/devices` |
| PD-04 | Tab 报表列表 + 下载 | 表格与确认 Modal | `GET /api/projects/:id/reports` + 下载 URL |
| PD-05 | Tab 可信档案：生成/核验/上链 | 三动作按钮 | 档案相关 API（见 4.11） |
| PD-06 | 打印 / 导出资料 / 采集上报 | 操作反馈 | 导出任务、采集工单接口 |
| PD-07 | 跳转 AI 验真 | 路由跳转（可带 projectId） | — |
| PD-08 | 无效 ID 空态 | 「未找到该项目」 | 404 |

#### 验收标准

- 设备/告警/报表与项目正确关联  
- 档案动作可写入可查询记录

---

### 4.5 告警中心 `/alarms`（P0）

#### 功能描述

安全、质量、进度、设备异常统一管控：搜索、筛选、确认处理、详情与处置建议。

#### 功能清单

| 编号 | 功能点 | 前端 | 后端 |
|------|--------|------|------|
| AL-01 | 告警列表 | 分级色标展示 | `GET /api/alarms` |
| AL-02 | 关键词搜索（内容/项目名） | 搜索框 | `?keyword=` |
| AL-03 | 处理状态筛选：全部/未处理/已处理 | Tab | `?acknowledged=` |
| AL-04 | 确认处理 | 更新本地态 + 提示 | `PATCH /api/alarms/:id/ack` |
| AL-05 | 详情 Modal（含处置建议） | Modal | 详情接口；建议可由规则返回 |
| AL-06 | 展示抓拍图（建议补齐） | 图片区 | `image` 字段 |

#### 列表字段

`projectName, level, message, deviceName, timestamp, acknowledged`  
建议补充：`type, image`

#### 验收标准

- 确认后状态持久化，刷新仍为已处理  
- 侧边栏未处理数量与真实未确认数一致（替换写死 badge）

---

### 4.6 设备监控 `/devices`（P0）

#### 功能描述

多源设备清单：类型筛选、状态/心跳/电量、批量诊断、详情与远程巡检。

#### 功能清单

| 编号 | 功能点 | 前端 | 后端 |
|------|--------|------|------|
| DV-01 | 设备表格 | 名称/类型/位置/状态/电量或心跳 | `GET /api/devices` |
| DV-02 | 类型筛选 | 全部/摄像头/传感器/边缘盒/安全帽 | `?type=` |
| DV-03 | 批量诊断 | 触发任务 | `POST /api/devices/diagnose` |
| DV-04 | 导出设备清单 | 下载文件 | `GET /api/devices/export` |
| DV-05 | 设备详情 Modal | 含所属项目 | `GET /api/devices/:id` |
| DV-06 | 发起远程巡检 | 下发任务 | `POST /api/devices/:id/inspect` |

#### 验收标准

- 心跳/在线状态可由边缘或设备接入服务更新  
- 离线超过阈值可自动生成设备类告警（后端规则）

---

### 4.7 数据分析 `/analytics`（P1）

#### 功能描述

多维趋势：近 7 日告警、项目进度对比、设备类型分布；支持导出分析报表。

#### 功能清单

| 编号 | 功能点 | 前端 | 后端 |
|------|--------|------|------|
| AN-01 | 7 日告警趋势图 | 柱图（安全/质量/设备） | `GET /api/analytics/alarm-trend` |
| AN-02 | 项目进度对比图 | 横向条 | `GET /api/analytics/project-progress` |
| AN-03 | 设备类型分布 | 卡片 | `GET /api/analytics/device-distribution` |
| AN-04 | 导出分析报表 | 触发 | `POST /api/reports/generate`（类型 analytics） |

---

### 4.8 报表中心 `/reports`（P1）

#### 功能描述

工程质量、安全、隐蔽核验、进度等报表的列表、预览、下载、批量导出与打印。

#### 功能清单

| 编号 | 功能点 | 前端 | 后端 |
|------|--------|------|------|
| RP-01 | 报表列表 | 类型/项目/周期/大小/状态 | `GET /api/reports` |
| RP-02 | 预览 Modal | 摘要内容 | `GET /api/reports/:id` |
| RP-03 | 单文件下载 | 下载 | 文件流 / 签名 URL |
| RP-04 | 导出全部 | 打包下载 | `GET /api/reports/export-all` |
| RP-05 | 打印 | 浏览器打印或 PDF | PDF（可选） |

#### 列表字段

`name, type, projectName, period, createdAt, size, status`  
建议展示：`createdBy`

---

### 4.9 GIS 地图 `/gis-map`（P1）

#### 功能描述

施工点位地图联动：选中点位查看进度/摄像头/告警，跳转项目详情与 AI 验真，生成对齐快照。

#### 功能清单

| 编号 | 功能点 | 前端 | 后端 |
|------|--------|------|------|
| GIS-01 | 地图点位渲染 | 地图 SDK 或示意层 | `GET /api/gis/markers`（含经纬度） |
| GIS-02 | 点位选中详情 | 右侧面板 | 关联项目详情字段 |
| GIS-03 | 跳转项目详情 / AI 验真 | 路由 | — |
| GIS-04 | 生成 GIS 对齐快照 | 按钮 | `POST /api/gis/snapshots` |
| GIS-05 | 刷新图层 | 重新拉取 | 同上 markers 接口 |
| GIS-06 | Banner 统计 | 活跃项目/校准点位/偏差告警 | `GET /api/gis/stats` |

#### 补充字段建议

Marker：`id, projectId, lng, lat, calibrated, deviationLevel`

---

### 4.10 数据看板 `/data-cockpit`（P1）

#### 功能描述

驾驶舱：四大 KPI、日/周/月运行指数、设备接入分布、告警结构占比、导出快照。

#### 功能清单

| 编号 | 功能点 | 前端 | 后端 |
|------|--------|------|------|
| CK-01 | KPI：在建项目、在线设备、今日告警、完成率 | 卡片 | 复用 dashboard stats |
| CK-02 | 综合运行指数（日/周/月） | 切换柱图 | `GET /api/cockpit/metrics?period=` |
| CK-03 | 设备接入分布 | 进度条 | device-distribution |
| CK-04 | 告警结构占比 | 占比图 | 告警聚合 |
| CK-05 | 导出驾驶舱快照 | 下载/入报表 | `POST /api/cockpit/snapshot` |

---

### 4.11 溯源查询 `/traceability`（P0）

#### 功能描述

按档案编号或 SHA-256 核验数字档案真伪，查看全流程时间线，复制哈希与导出核验单。

#### 功能清单

| 编号 | 功能点 | 前端 | 后端 |
|------|--------|------|------|
| TR-01 | 检索输入（编号/哈希） | 搜索框 | `GET /api/archives?q=` |
| TR-02 | 核验真伪 | 结果区展示 | `POST /api/archives/verify` |
| TR-03 | 完整时间线 Modal | timeline 列表 | 档案详情含 timeline |
| TR-04 | 复制哈希 | 剪贴板 | — |
| TR-05 | 导出核验单 | 下载 PDF | `GET /api/archives/:id/certificate` |
| TR-06 | 命中列表快捷选中 | 点击填充 | 搜索结果列表 |

#### 档案字段

`id, project, hash, timestamp, status, location, summary, timeline[]`

#### 验收标准

- AI 上链 / 项目档案生成后，可在本页检索到  
- 篡改哈希应返回核验失败（后端校验）

---

### 4.12 模型服务 `/model-service`（P1）

#### 功能描述

端边云模型编排：列表与详情、启停、Demo 推理、版本切换、压测、下发边缘、健康度与报表。

#### 功能清单

| 编号 | 功能点 | 前端 | 后端 |
|------|--------|------|------|
| MD-01 | 模型列表与选中详情 | 列表+面板 | `GET /api/models` |
| MD-02 | 启停控制 | running ↔ idle | `POST /api/models/:id/start` `/stop` |
| MD-03 | Demo 推理预览 Modal | 展示输出摘要 | `POST /api/models/:id/infer` |
| MD-04 | 版本切换 | 选择版本 | `POST /api/models/:id/switch-version` |
| MD-05 | 性能压测 | 触发任务 | `POST /api/models/:id/benchmark` |
| MD-06 | 下发边缘节点 | 下发任务 | `POST /api/models/:id/deploy-edge` |
| MD-07 | 健康度指标 | 实例数/健康率/待调度 | `GET /api/models/health` |
| MD-08 | 导出服务报表 | 下载 | 报表接口 |

#### 模型示例场景（与现网 Demo 对齐）

1. 边缘违章检测  
2. 隐蔽工程结构化验真  
3. 图物对齐进度核算  
4. 弱网任务补偿调度  

---

### 4.13 账户设置 `/settings/account`（P2）

| 编号 | 功能点 | 前端 | 后端 |
|------|--------|------|------|
| U-01 | 编辑姓名/手机/邮箱/部门/岗位 | 表单 | `GET/PUT /api/users/me` |
| U-02 | 更换头像 | 上传 | `POST /api/users/me/avatar` |
| U-03 | 通知偏好四开关 | Toggle | `PUT /api/users/me/notify-prefs` |
| U-04 | 重置密码 | 触发邮件/流程 | `POST /api/users/me/reset-password` |
| U-05 | 展示最近登录、2FA 状态 | 只读 | 用户安全信息接口 |

---

### 4.14 系统设置 `/settings/system`（P2）

| 编号 | 功能点 | 前端 | 后端 |
|------|--------|------|------|
| S-01 | 协同模式选择 | select | `GET/PUT /api/system/config` |
| S-02 | 数据保留天数 | number input | 同上 |
| S-03 | 日志等级 | select | 同上 |
| S-04 | 开关：弱网缓存、审计日志、边缘缓存、自动归档 | Toggle | 同上 |
| S-05 | 测试数据库连通 | 按钮 | `POST /api/system/test/db` |
| S-06 | 测试边缘节点 | 按钮 | `POST /api/system/test/edge` |
| S-07 | 导出审计日志 | 下载 | `GET /api/system/audit-logs/export` |
| S-08 | 健康巡检 | 按钮 | `POST /api/system/health-check` |
| S-09 | 资源占用展示 | CPU/内存/存储 | `GET /api/system/resources` |
| S-10 | 立即备份 | 按钮 | `POST /api/system/backup` |

---

## 5. 建议后端模块划分

| 服务/模块 | 职责 | 主要实体 |
|-----------|------|----------|
| 认证与用户 | 登录、权限、账户与通知偏好 | User, Role, Notification |
| 项目服务 | 项目 CRUD、阶段进度 | Project |
| 设备接入 | 设备注册、心跳、巡检、诊断 | Device |
| 告警服务 | 告警入库、确认、规则生成 | Alarm |
| AI 验真服务 | 任务队列、推理、结构化结果 | AIVerification |
| 存证溯源 | 哈希存证、档案、核验 | DigitalArchive |
| GIS 服务 | 点位、对齐快照 | Marker, Snapshot |
| 报表与分析 | 报表生成、图表聚合 | Report, Metrics |
| 模型编排 | 模型启停、下发、压测 | ModelService |
| 系统运维 | 配置、健康检查、备份、审计 | SystemConfig |

---

## 6. 建议开发分期

### 一期（可演示联调，对齐 P0）

1. 登录鉴权（最小可用）  
2. 项目 CRUD + 详情  
3. 设备列表/心跳模拟 + 告警确认  
4. AI 验真任务（可先接 Mock 推理，再接真实模型）  
5. 档案存证与溯源查询  
6. Dashboard 聚合接口  

### 二期（P1）

1. 报表生成与文件存储  
2. 数据分析 / 数据看板真实聚合  
3. GIS 真实地图与坐标  
4. 模型服务启停与边缘下发  

### 三期（P2 + 增强）

1. 账户/系统设置全量  
2. 全局搜索与通知中心  
3. 弱网续传、审计、备份等运维能力  
4. 权限细粒度与多租户（如需要）  

---

## 7. 前后端联调约定（建议）

1. **统一响应结构**：`{ code, message, data }`  
2. **分页**：`page, pageSize, total`  
3. **时间**：ISO 8601，前端本地化展示  
4. **枚举**：与本文档 / `src/types/index.ts` 保持一致  
5. **文件**：上传走对象存储，接口返回 URL  
6. **长任务**：AI 分析、报表生成、压测等用任务 ID + 轮询或 WebSocket  
7. **替换 Demo**：前端逐步将 `src/data/mock.ts` 与页面内硬编码改为 API 调用，交互成功态从 `Notice` 模拟改为真实结果

---

## 8. 附录：路由与状态枚举速查

### 路由

```
/dashboard
/ai-verification
/projects
/projects/:id
/alarms
/devices
/analytics
/reports
/gis-map
/data-cockpit
/traceability
/model-service
/settings/account
/settings/system
```

### 状态文案映射

| 枚举 | 中文 |
|------|------|
| Project.active | 进行中 |
| Project.pending | 待启动 |
| Project.paused | 已暂停 |
| Project.completed | 已完成 |
| Alarm.critical / warn / info | 严重 / 警告 / 信息 |
| Device.online / offline / warning / fault | 在线 / 离线 / 预警 / 故障 |
| AI pass / warn / fail | 通过 / 需复检 / 不通过 |
| Report.generated / pending | 已生成 / 生成中 |
| Model.running / idle / updating | 运行中 / 空闲 / 升级中 |

---

**文档结束。** 可按模块直接拆任务：前端对接接口、后端按实体建表与 API。若需要，可再输出《接口明细表（OpenAPI）》或《数据库表结构设计》。
