# Label spec: contract example v0

This file exists only to demonstrate digest binding.

- `helmet_compliant`: the single synthetic person drawing includes a helmet symbol.
- `helmet_missing`: the single synthetic person drawing does not include a helmet symbol.

These labels are not field annotations and do not establish a real model capability.
