# Metric spec: contract example v0

Closed-set single-label accuracy on exactly two synthetic validation cases.

The threshold field remains `0.85` because the v0 schema is fixed, but this fixture is deliberately below threshold and is never eligible for a compliance claim.
